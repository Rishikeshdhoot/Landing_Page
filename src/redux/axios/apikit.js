import axios from "axios";
// // import { DataManager } from "../../helper/dataManager";

// // export const url = "http://192.168.3.175:5001/"  //local
// export const url = "http://127.0.0.1:5000/api/v1/"; // dell
// // export const url = "http://1.6.98.139:4007/" // Public

// export const finalURL = `${url}api/v1`;
// const APIKit = axios.create({
//   baseURL: finalURL,
//   timeout: 60000000,
// });

// APIKit.interceptors.request.use(async (config) => {
//   let accessToken = await DataManager.getAccessToken();
//   if (accessToken) {
    // config.headers["x-access-token"] = `Bearer ${accessToken}`;
//   }
//   return config;
// });

// export default APIKit;
